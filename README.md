# GPT Helper - Chrome Extension

Модульное расширение для Chrome, которое использует OpenAI GPT API для генерации ответов на основе выбранного текста на странице. Расширение имеет встроенный UI на странице и модульную архитектуру для легкого добавления новых функций.

## ⚠️ ВАЖНО ДЛЯ РАЗРАБОТЧИКОВ

**Перед любыми изменениями в коде ОБЯЗАТЕЛЬНО прочитайте [WORKFLOW.md](WORKFLOW.md)**

Этот файл содержит критические правила работы расширения, которые НЕЛЬЗЯ нарушать:
- Как должен работать выбор элементов
- Как формируется запрос к OpenAI (промпты, база данных, настройки)
- Что должно сохраняться и когда
- Какие параметры передавать между модулями

**Несоблюдение этих правил ломает функциональность расширения!**

## 📋 Содержание

- [Быстрый старт](#быстрый-старт)
- [Архитектура](#архитектура)
- [Структура файлов](#структура-файлов)
- [API модулей](#api-модулей)
- [Хранилище данных](#хранилище-данных)
- [Создание новых модулей](#создание-новых-модулей)
- [Решение проблем](#решение-проблем)

## 🚀 Быстрый старт

### Установка

1. Откройте Chrome и перейдите в `chrome://extensions/`
2. Включите **"Режим разработчика"** в правом верхнем углу
3. Нажмите **"Загрузить распакованное расширение"**
4. Выберите папку `auto-answer`

### Первичная настройка

1. Нажмите на иконку расширения в панели инструментов Chrome
2. Нажмите на иконку ⚙️ (Настройки) в правом верхнем углу popup
3. Введите ваш API ключ OpenAI (начинается с `sk-`)
4. Нажмите **"Проверить подключение"** для проверки ключа
5. Нажмите **"Сохранить настройки"** и закройте popup

### Использование

1. На любой странице в браузере нажмите **"Выбрать элемент"** (стрелка) в расширении
2. Кликните на текст на странице, который нужно обработать
3. После выбора появится плавающая кнопка на странице
4. Заполните промпты и нажмите **"Сгенерировать ответ"**
5. Скопируйте результат и вставьте где нужно

### Получение API ключа

1. Перейдите на [platform.openai.com](https://platform.openai.com)
2. Создайте аккаунт или авторизуйтесь
3. Перейдите в [API keys](https://platform.openai.com/api-keys)
4. Нажмите **"Create new secret key"**
5. Скопируйте ключ и введите в расширение

## 🏗️ Архитектура

Расширение использует **модульную архитектуру**, похожую на WordPress plugins:
- **popup.js** - основной контроллер и загрузчик модулей
- **modules/{name}/** - каждый модуль это отдельная папка с HTML, JS, CSS
- **background.js** - service worker для обработки сообщений
- **content.js** - инъекция UI в DOM страницы

### Особенности архитектуры:

1. **Динамическая загрузка** - модули загружаются при открытии popup
2. **Изоляция кода** - каждый модуль имеет свои стили и логику
3. **ES6 modules** - используются нативные ES6 import/export
4. **Event-based communication** - модули общаются через события

## 📁 Структура файлов

```
auto-answer/
│
├── manifest.json              # Конфигурация Chrome Extension Manifest v3
├── background.js              # Service worker (message relay, selector, API)
├── content.js                 # Content script (floating button, modal, API)
├── popup.html                 # Главное окно расширения (dropdown + tabs)
├── popup.js                   # Контроллер popup (модульный загрузчик)
├── popup.css                  # Глобальные стили popup
│
├── modules/                   # Модульная архитектура
│   └── answer/                # Модуль "Ответить"
│       ├── answer.html        # Контент вкладки (селектор, поля, кнопки)
│       ├── answer.js          # Логика модуля (export initAnswerModule)
│       ├── answer.css         # Стили модуля (settings, inputs, buttons)
│       └── history.json       # Шаблон для хранения истории ответов
│
├── icons/                     # Иконки расширения
├── README.md                  # Этот файл
└── TaxFreeCorp.code-workspace # VS Code workspace config
```

## 🔌 API модулей

### Как загружается модуль

Когда открывается popup, `popup.js` автоматически:

```javascript
// 1. Загружает HTML из modules/{name}/{name}.html
// 2. Вставляет в <div id="tab-{name}" class="tab-content">
// 3. Импортирует modules/{name}/{name}.js
// 4. Вызывает export function initAnswerModule(...)
```

### Сигнатура инициализации модуля

```javascript
export function initAnswerModule(
  elements,           // Объект DOM элементов для этого модуля
  showSelectorStatus, // Функция(message, type) для вывода статуса
  hideSelectorStatus, // Функция() для скрытия статуса
  showGeneralStatus,  // Функция(type, message) для общих сообщений
  setButtonLoading    // Функция(button, isLoading) для состояния кнопок
) {
  // Инициализация логики модуля
  // Установка обработчиков событий
  // Загрузка сохраненных данных
}
```

### Доступные функции-помощники (из popup.js):

```javascript
// Показать статус выбора элемента
showSelectorStatus('Сообщение', 'info|success|error');

// Скрыть статус
hideSelectorStatus();

// Показать общее сообщение
showGeneralStatus('error|success', 'Текст сообщения');

// Управление состоянием кнопки (загрузка)
setButtonLoading(button, true|false);
```

### События для контроля селектора элементов:

Когда пользователь выбрал элемент через селектор:

```javascript
// Модуль получит событие:
window.addEventListener('elementSelected', (event) => {
  const { text } = event.detail;
  // text содержит текст выбранного элемента
});
```

Для управления селектором:

```javascript
// Включить селектор
chrome.runtime.sendMessage({ action: 'enableSelector' }, (response) => {
  // response.success === true если успешно
});

// Отключить селектор
chrome.runtime.sendMessage({ action: 'disableSelector' }, () => {});
```

## 💾 Хранилище данных

Расширение использует Chrome Storage API (`chrome.storage.local`) для сохранения:

### Ключи хранилища:

| Ключ | Тип | Описание |
|------|-----|---------|
| `apiKey` | string | OpenAI API ключ (секретный) |
| `lastSelectedText` | string | Последний выбранный текст на странице |
| `lastGeneratedAnswer` | string | Последний сгенерированный ответ |
| `positivePrompt` | string | Положительный промпт для модели |
| `negativePrompt` | string | Отрицательный промпт для модели |
| `baseContent` | string | Содержимое загруженного знаний-базы файла |
| `baseFileName` | string | Имя файла знаний-базы |

### Пример работы с хранилищем:

```javascript
// Чтение из хранилища
const data = await chrome.storage.local.get(['apiKey', 'lastSelectedText']);
console.log(data.apiKey, data.lastSelectedText);

// Запись в хранилище
await chrome.storage.local.set({
  lastSelectedText: 'новый текст',
  positivePrompt: 'новый промпт'
});

// Удаление из хранилища
await chrome.storage.local.remove(['lastSelectedText']);

// Очистка всего хранилища
await chrome.storage.local.clear();
```

## 🆕 Создание новых модулей

### Шаг 1: Создайте структуру папки

```
modules/my-module/
├── my-module.html      # HTML контент
├── my-module.js        # Логика ES6 экспорт
└── my-module.css       # Стили
```

### Шаг 2: Создайте HTML (my-module.html)

```html
<!-- my-module.html -->
<div class="section">
  <button id="myButton" class="btn btn-primary">
    <span class="btn-icon">
      <i class="fas fa-star"></i>
    </span>
    <span class="btn-text">Моя кнопка</span>
  </button>
</div>

<div class="section">
  <textarea id="myInput" class="textarea" placeholder="Введите текст..."></textarea>
</div>
```

### Шаг 3: Создайте JS логику (my-module.js)

```javascript
// my-module.js
export function initMyModuleModule(
  elements,
  showSelectorStatus,
  hideSelectorStatus,
  showGeneralStatus,
  setButtonLoading
) {
  // Получить элементы вашего модуля
  const myButton = document.getElementById('myButton');
  const myInput = document.getElementById('myInput');

  // Добавить обработчик события
  myButton.addEventListener('click', async () => {
    setButtonLoading(myButton, true);

    try {
      // Ваша логика здесь
      const text = myInput.value;
      showGeneralStatus('success', 'Обработано!');
    } catch (error) {
      showGeneralStatus('error', `Ошибка: ${error.message}`);
    } finally {
      setButtonLoading(myButton, false);
    }
  });

  // Загрузить сохраненные данные если нужно
  chrome.storage.local.get(['myModuleData'], (data) => {
    if (data.myModuleData) {
      myInput.value = data.myModuleData;
    }
  });
}
```

### Шаг 4: Добавьте стили (my-module.css)

```css
/* my-module.css */
.my-module-special {
  color: #667eea;
  font-weight: bold;
}
```

### Шаг 5: Обновите popup.html

Добавьте пункт в dropdown:

```html
<!-- В section "Dropdown items" в popup.html -->
<a href="#" class="dropdown-item" data-tab="my-module">
  <i class="fas fa-star"></i>
  <span>Мой модуль</span>
</a>
```

И добавьте контейнер для контента:

```html
<!-- В section "Tab contents" в popup.html -->
<div id="tab-my-module" class="tab-content"></div>
```

Дополнительно добавьте стили:

```html
<!-- В <head> после answer.css -->
<link rel="stylesheet" href="modules/my-module/my-module.css">
```

### Шаг 6: Обновите popup.js

В функции `loadModule()` в `DOMContentLoaded` добавьте:

```javascript
// После: await loadModule('answer');
await loadModule('my-module');
```

## 🔧 Решение проблем

### Проблема: "Модуль не загружается"

**Решение:**
- Проверьте консоль (F12 → Console)
- Убедитесь, что HTML файл находится по пути: `modules/{name}/{name}.html`
- Проверьте, что JS файл экспортирует функцию: `export function init{Name}Module(...)`

### Проблема: "Селектор не работает"

**Решение:**
- Перезагрузите расширение на странице `chrome://extensions/`
- Убедитесь, что у расширения есть разрешение на текущей странице
- Проверьте, что `content.js` работает в консоли страницы (не popup консоли)

### Проблема: "API ключ не сохраняется"

**Решение:**
- Проверьте, что ключ начинается с `sk-`
- Откройте DevTools расширения и проверьте `chrome.storage.local`
- Попробуйте очистить хранилище и повторить сохранение

### Отладка

**Открыть консоль расширения:**
1. `chrome://extensions/`
2. Найдите "GPT Helper"
3. Нажмите "Inspect views: service worker" или "Inspect popup"

**Посмотреть хранилище в консоли:**
```javascript
// В консоли любого скрипта расширения
chrome.storage.local.get(null, (data) => console.log(data));
```

**Отключить расширение временно:**
```javascript
// На странице в DevTools консоли:
chrome.runtime.sendMessage({ action: 'disableSelector' });
```

## 📊 Поток данных

```
Пользователь на странице
        ↓
[Нажимает "Выбрать элемент"]
        ↓
content.js (enableSelector → оверлей на странице)
        ↓
[Кликает на элемент]
        ↓
background.js (сохраняет текст в storage)
        ↓
popup.js (получает сообщение, диспетчит событие)
        ↓
answer.js модуль (слушает elementSelected событие)
        ↓
textarea заполняется текстом элемента
        ↓
[Пользователь нажимает "Сгенерировать"]
        ↓
content.js + background.js (вызывает OpenAI API)
        ↓
Результат отображается в textarea
        + сохраняется в chrome.storage.local
```

## 📝 Планы развития

- [ ] Реализовать историю ответов (modules/answer/history.json)
- [ ] Добавить модуль "Переводчик"
- [ ] Добавить модуль "Улучшитель текста"
- [ ] Добавить модуль "Проверка грамматики"
- [ ] Поддержка разных моделей GPT (GPT-4, Claude и т.д.)
- [ ] Экспорт истории в CSV/JSON
- [ ] Пользовательские промпты (встроенные шаблоны)
- [ ] Темная тема для UI

## 🐛 Известные проблемы

- Расширение закрывает popup при выборе элемента (это нормально, для удобства)
- API ключ хранится в plain text в Storage (используйте защищенное хранилище для production)
- Максимум 4096 токенов в ответе (ограничение API)

## 📄 Лицензия

MIT License - Используйте свободно в личных и коммерческих проектах
